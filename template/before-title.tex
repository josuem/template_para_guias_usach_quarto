% Importaciones propias
\usepackage{fancyhdr}
\usepackage{lipsum}  
\usepackage{geometry}
\geometry{
  letterpaper,
  left=20mm,
  right=20mm,
  headheight=3.5cm,
  top=4.0cm,
  bottom=2.0cm,
  footskip=1cm
}

%% Defino firasans como variable
\usepackage{fontspec}
$if(firasans)$
  \setmainfont{Fira Sans}
  \setsansfont{Fira Sans}
  
  % Para instalar Fira Math buscar e instalar firamath en miktex
  \setmathfont{Fira Math}  
$endif$


%% Variables propias
% Nombre del curso
$if(curso)$
  \newcommand{\curso}{$curso$}
$endif$

% Información de la universidad
$if(universidad)$
\newcommand{\universidad}{
  $for(universidad)$
  $universidad$\\
  $endfor$
  }
$endif$

% Información del laboratorio
$if(laboratorio)$
\newcommand{\laboratorio}{
  $for(laboratorio)$
  $laboratorio$\\
  $endfor$
  }
$endif$

% Definición de formato
\definecolor{greenPortada}{HTML}{69A84F}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{2pt}
\renewcommand{\headrulewidth}{1.5pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{greenPortada}\leaders\hrule height \headrulewidth\hfill}}
\renewcommand{\footrule}{\hbox to\headwidth{\color{greenPortada}\leaders\hrule height \footrulewidth\hfill}}